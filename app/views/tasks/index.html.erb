<h2>Here is a list of all the tasks that you have</h2>

<ul>
  <% @tasks.each do |task|%>
    <li>
      <%= task.title%>
      <%= link_to "Delete", task_path(task.id), method: :delete %>
      <%= link_to task.title, task_path(task.id)%>
    </li>
  <%end%>
</ul>


<p><%=link_to "create new task", new_path%></p>
